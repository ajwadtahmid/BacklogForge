// ── Animation durations ───────────────────────────────────────────────────────
const kAnimFast   = Duration(milliseconds: 150);
const kAnimNormal = Duration(milliseconds: 200);

// ── Artwork thumbnail sizes ───────────────────────────────────────────────────

/// Game list row thumbnail (96 × 54 px — 16:9 aspect).
const kArtworkCardW  = 96.0;
const kArtworkCardH  = 54.0;

/// Pick-card artwork (120 × 90 px — 4:3 aspect).
const kArtworkPickW  = 120.0;
const kArtworkPickH  = 90.0;

/// Search-result / compact row thumbnail (72 × 40 px).
const kArtworkSearchW = 72.0;
const kArtworkSearchH = 40.0;

// ── Typography ────────────────────────────────────────────────────────────────

/// Hero title overlaid on the detail-screen artwork header.
const kDetailTitleSize = 22.0;
